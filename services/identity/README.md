# Identity service (MVP)

The vertical-slice platform in `cmd/platform` handles identity auth for local development.

Run: `./scripts/migrate-and-run.sh` or `go run ./cmd/platform`

Migrations: `services/identity/migrations/`
