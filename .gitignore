# Dependencies
node_modules/
dist/
build/
.turbo/
*.exe
*.test
bin/
coverage/
.venv/
__pycache__/

# Compiled Go binaries
/platform

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Env & secrets
.env
.env.*
!.env.example
*.pem

# Go
vendor/

# Android
apps/android/.gradle/
apps/android/**/build/
apps/android/local.properties
*.apk
*.aab

# Logs / data
*.log
# Anchored: a bare 'data/' also matched every Android module's data package
# (com.techlane.*.data), silently keeping repositories, Room and the token
# stores out of the repo.
/data/
tmp/

# Generated clients (optional keep)
# contracts/generated/

# Server-only static assets (never rsync --delete without preserving)
/web-dist/

# Claude Code local config — contains machine-specific SSH/host details
.claude/
